# Showcase video

Committed public storyboard:

`showcase-2min.mp4`

Wired in `docs/index.html` as `assets/video/showcase-2min.mp4` (relative to `docs/`).

This is an ~80s text-card cut of the Showcase beats (mascot → objective → `@` handoffs → README → live-run → PR/protocol). Footer on every frame is `Hackathon-Builder-Grok_Bot`. Replace with a live-room recording when available; keep the same filename so Pages keeps playing.
